import styled from 'styled-components';
import { Card } from 'antd';

export const ErrorMsg = styled.div`
    && {
        color: red;
    }
`;
export const EditNicknameCard = styled(Card)`
    && {
        margin-bottom: 10px;
    }
`;